query Popular($media: String!, $params: PopularParams) {
  popular(media: $media, params: $params) {
    page
    total_pages
    results {
      adult
      backdrop_path
      genre_ids
      id
      name
      overview
      poster_path
      release_date
      media_type
    }
  }
}
