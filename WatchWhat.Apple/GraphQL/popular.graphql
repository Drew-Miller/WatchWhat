query Popular($media: String!) {
  popular(media: $media) {
    page
    total_pages
    results {
      adult
      backdrop_path
      genre_ids
      id
      name
      overview
      poster_path
      release_date
      mediaType
    }
  }
}
