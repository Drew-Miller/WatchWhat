query Similar($media: String!, $id: Int!) {
  similar(media: $media, id: $id) {
    page
    total_pages
    results {
      adult
      backdrop_path
      genre_ids
      id
      name
      overview
      poster_path
      release_date
      mediaType
    }
  }
}
