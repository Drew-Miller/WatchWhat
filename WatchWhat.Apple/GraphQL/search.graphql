query Search($media: String!, $params: SearchParams!) {
  search(media: $media, params: $params) {
    page
    total_pages
    total_results
    results {
      adult
      backdrop_path
      genre_ids
      id
      name
      overview
      poster_path
      release_date
      media_type
    }
  }
}
