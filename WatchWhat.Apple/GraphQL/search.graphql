query Search($query: String!, $page: Int) {
  searchMovies(query: $query, page: $page) {
    page,
    total_pages,
    total_results,
    results {
      id
      title
      poster_path
      backdrop_path
    }
  }
}
