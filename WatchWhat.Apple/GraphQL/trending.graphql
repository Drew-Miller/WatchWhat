query Trending($media: String!, $time: String!) {
  trending(media: $media, time: $time) {
    page
    total_pages
    results {
      adult
      backdrop_path
      genre_ids
      id
      name
      overview
      poster_path
      release_date
      mediaType
    }
  }
}
